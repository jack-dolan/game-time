services:
  letsgogaming:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
      # CORS allowlist Socket.io checks the browser's origin against.
      - ALLOWED_ORIGINS=http://localhost:8080
